# --- SERVER-SIDE ONLY (Private) ---
# Used for Prisma migrations and server-side fetching
DATABASE_URL="postgresql://user:password@localhost:5432/orbit_db"

# --- CLIENT-SIDE ACCESSIBLE (Public) ---
# Accessible to your offline components and PWA logic
NEXT_PUBLIC_APP_VERSION="1.0.0-beta"
NEXT_PUBLIC_OFFLINE_MODE_DEFAULT="true"